name: CI/CD

on:
  push:
    branches:
      - dev
      - main

jobs:
  build:
    if: github.ref == 'refs/heads/dev'
    runs-on: ubuntu-latest
    steps:
      - name: Build for dev
        run: echo "Dev build running"

  deploy:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to production
        run: echo "Main deploy running"
